package org.reins.url.repository;
import org.reins.url.entity.Edit_log;
import org.springframework.data.jpa.repository.JpaRepository;
public interface Edit_logRepository extends JpaRepository<Edit_log,Long> {
}
